# GTK, PyGObject, pycairo, PyYAML, and PyInstaller are installed as exact
# MSYS2 UCRT64 packages by the root-owned build workflow. PyEnchant is the only
# PyPI input; its source archive hash is pinned in this included lock file.
-r pyenchant-source-lock.txt
